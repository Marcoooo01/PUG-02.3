doctype html
html
 head
   title #{title}
   meta(name="viewport" content="width=device-width, initial-scale=1")
 body
   header
         h1 #{title}
   div
     img(src=`/img/${c.Istruzioni.Link1}`)
     br
     img(src=`/img/${c.Istruzioni.Link2}`)
     br
     img(src=`/img/${c.Istruzioni.Link3}`)