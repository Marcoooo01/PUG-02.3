doctype html
html
 head
   title #{title}
   meta(name="viewport" content="width=device-width, initial-scale=1")
 body
   header
         h1 #{title}
   div
     each immagine in ca.Istruzioni
       img(src=`img/${immagine}`)
     
     